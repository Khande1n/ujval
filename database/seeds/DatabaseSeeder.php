<?php

use Illuminate\Database\Seeder;
use Illuminate\Database\Eloquent\Model;
use App\Student;
use App\User;
use App\Exam;


class DatabaseSeeder extends Seeder
{
	
	// protected $toTruncate = ['users'];
	
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // Model::unguard(); 
// 		
		// foreach($this->toTruncate as $tables){
			// DB::table($tables)->truncate();
		// }
// 		
		// $this->call('UsersTableSeeder');
		
		// foreach(range(1, 100) as $index)
    	// {
        	// DB::table('grade_student')->insert([
            	// 'grade_id' => rand(1,20),
            	// 'student_id' => $faker->unique()->randomNumber(1, 100)
        	// ]);
    	// }
// 		
		// foreach(range(1, 20) as $index)
    	// {
        	// DB::table('grade_subject')->insert([
            	// 'grade_id' => rand(1,20),
            	// 'subject_id' => $faker->unique()->randomNumber(1, 20)
        	// ]);
    	// }
// 		
		// foreach(range(1, 50) as $index)
    	// {
        	// DB::table('grade_user')->insert([
            	// 'grade_id' => rand(1,20),
            	// 'user_id' => $faker->unique()->randomNumber(1, 50)
        	// ]);
    	// }
// 		
		// foreach(range(1, 50) as $index)
    	// {
        	// DB::table('role_user')->insert([
            	// 'role_id' => rand(1,3),
            	// 'user_id' => $faker->unique()->randomNumber(1, 50)
        	// ]);
    	// }
// 		
		// foreach(range(1, 100) as $index)
    	// {
        	// DB::table('school_student')->insert([
            	// 'school_id' => rand(1,3),
            	// 'student_id' => $faker->unique()->randomNumber(1, 100)
        	// ]);
    	// }
// 		
		// foreach(range(1, 50) as $index)
    	// {
        	// DB::table('school_user')->insert([
            	// 'school_id' => rand(1,3),
            	// 'user_id' => $faker->unique()->randomNumber(1, 50)
        	// ]);
    	// }
// 		
		// foreach(range(1, 50) as $index)
    	// {
        	// DB::table('subject_user')->insert([
            	// 'subject_id' => rand(1,20),
            	// 'user_id' => $faker->unique()->randomNumber(1, 50)
        	// ]);
    	// }
	}
}
